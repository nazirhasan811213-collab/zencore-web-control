{
  "name": "zencore-web-control",
  "version": "1.0.0",
  "private": true,
  "description": "ZenCore TradingView Pine Script webhook dashboard",
  "scripts": {
    "start": "node -r ./compat-v17.js server-analysis.js",
    "test": "node --test test/*.test.js",
    "test:secure-pod": "python -m unittest mt5-secure-pod/test_secure_pod.py",
    "test:hosted-worker": "PYTHONPATH=hosted-mt5-worker python -m unittest discover -s hosted-mt5-worker -p 'test_*.py'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
