let zcAlerts=[];const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function renderAlertTape(){const el=document.getElementById('alertTape');if(!el)return;const rows=zcAlerts.slice(-40).reverse();el.innerHTML=rows.length?rows.map(a=>`<div style="padding:8px 0;border-bottom:1px solid #ffffff0c"><div style="font-size:9px;color:#7c8ea7">${new Date(a.time||Date.now()).toLocaleTimeString()}</div><div style="font-size:10px;line-height:1.45;margin-top:3px">${esc(a.message)}</div></div>`).join(''):'<div class="muted">No text alerts yet.</div>'}async function initAlertTape(){try{const r=await fetch('/api/alerts',{cache:'no-store'});zcAlerts=await r.json();if(!Array.isArray(zcAlerts))zcAlerts=[]}catch(_){zcAlerts=[]}renderAlertTape();try{const es=new EventSource('/events');es.addEventListener('alert',e=>{try{zcAlerts.push(JSON.parse(e.data));if(zcAlerts.length>100)zcAlerts=zcAlerts.slice(-100);renderAlertTape()}catch(_){}})}catch(_){}}initAlertTape();